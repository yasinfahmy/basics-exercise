name: 
  Deploy Project
on:
  push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
        # Download Code    
        - name: Get code
          uses: actions/checkout@v3
        - name: Install dependencies
          run: npm ci
        - name: Lint
          run: npm lint
        - name: Test
          run: npm test
        - name: Build
          run: npm build
        - name: Deploy code
          run: echo "Deploying..."